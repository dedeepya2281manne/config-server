server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dedeepya2281manne/configuration
          username: ${GIT_USERNAME}
          password: ${GIT_TOKEN}
          clone-on-start: true
